node_modules/

# Auth/session capture artifacts — contain live tokens and cookies
capture/

# CLI session cookies
session.json

# Go build and release artifacts
/timesheet
/timesheet.exe
/timesheet-cli
/dist/
/coverage.out
